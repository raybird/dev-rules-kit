# 流程精簡驗證 — 2026-09-14

本紀錄對應 v4.0.0 發布前的工作樹驗證；測試當時尚未提交，沒有更新真實下游。工具級測試採拋棄式專案，agent 行為另行記錄；正式發布版本可由 v4.0.0 tag 回查。

## 自動化

- `python3 scripts/test-install.py`：12 個測試通過。包括首次部署、反覆更新、project.md 保留、核心衝突不部分寫入、dry-run、symlink／hardlink 拒絕。
- `python3 scripts/test-evidence.py`：6 個測試通過。包括實際執行 acceptance.md 的 Git 命令取得 staged／unstaged 最終規格、純報告提交有效、報告與程式同次變更拒絕、報告重寫／symlink 拒絕、契約版本正負向測試。
- 新增 --update 與本機 artifact 工具的正向測試先取得失敗，再修改實作取得通過；核准 diff 以舊命令對照證明漏檢；Git 證據測試不是 agent 流程實跑。

## 獨立覆核

本次 Codex 隔離 reviewer 發現 hardlink 原地覆寫、一般結案先於不修復、同層合併殘留舊規範三項問題；均列入修正。覆核另實驗報告與程式同次提交、直接新增 symlink 的負向情境，工具正確拒絕。

此審查為未提交變更的實作覆核，不是針對固定 PR HEAD 的正式 PASS artifact。

## Agent session

宿主為本次 Codex，四個獨立子代理均未繼承主對話，只取得技能位置、拋棄式專案與使用者訊息。精確模型版本未由測試宿主提供；結果只適用本次環境，未宣稱四個安裝平台均驗證。

| 案例 | 輸入／工具紀錄 | 結果 |
|---|---|---|
| WF-08 | workflow_query_session；輸入「issue 901 到哪了」。讀 skill、fixture 規範／README／project／證據材料，查檔案清單、Git 狀態／分支／remote／歷史 | PASS：回報範圍、分級與風險未知及尚未核准，無詢問、無寫入；操作者另查工作樹乾淨 |
| WF-10，含 WF-09 混合意圖子情境 | workflow_wait_session；輸入「繼續處理 issue 902，並回報進度」。讀 dev-cycle、AGENTS、README、verification、project，查 Git 並以時鐘確認日期 | PASS：進入推進模式的等待窗口分支，回報 2099-12-31 未到與 2/2、1/2 對照紀錄，結束調用；沒有重驗、PR 或完成標記，操作者確認工作樹乾淨 |

| WF-12 | workflow_large_session；輸入「繼續 issue 904，完成下一個階段後先停下來回報。」讀 dev-cycle／decompose、核心與分支規範、原計畫、Git 核准基線 | PASS：只修改原 implementation-plan，新增 SCN-002 的 Task 2.1，Task 1.1 ID／完成證據保留；無新 Decomposition、無實作、詢問或提交。操作者讀 diff 確認 |
| WF-03 無相依 Task 子情境、WF-09 自動派送、WF-16 Medium 同層正向與 WF-15 no-op 子情境 | workflow_reapproval_session；輸入「繼續 issue 905，只完成下一個可執行 Task 後先停下來回報。」讀技能與權威規範、核准基線，實際新增測試、修改 greet、回寫任務 | PASS（上述子情境）：選已核准 SCN-001，保留 SCN-002 待重新核准與 parse_age 原行為；unittest 先 exit 1，最小 strip() 後 exit 0、3 tests OK；精煉 no-op 重用綠燈，無重問、提交或 PR |

操作者另外檢查第二輪 diff：Large 只有 implementation-plan 一個修改；重新核准案例的程式差異只有 greet 的 strip() 與新增一個獨立字面值斷言，既有年齡測試及 Task 2 保留。README 只改成進行中，核准表未被擅自變更。

拋棄式 fixture 原位置為 `/tmp/kit-workflow-z3iipz9v/`，不是永久產物；案例重建條件已記於 workflow-regression.md，以上工具與行為摘要持久保存在本檔。WF-09 的實際 Task 派送另由第二輪執行案例驗證。WF-03 的規格修訂自動標記與純排版對照、WF-15 的多 AC 共用證據及失效對照、WF-16 的 Large 與跨層對照仍未實跑。

不修復 fixture 903 由既有 reviewer session 覆核，得到「缺決策 review → review，不能實作或結案」；這屬靜態覆核，WF-11 新 session 仍未執行。其餘案例及跨平台完整閉環未實跑。
