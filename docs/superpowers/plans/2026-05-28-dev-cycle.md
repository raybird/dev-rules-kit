# dev-cycle Skill 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `dev-cycle` orchestration skill，讓使用者能以 issue ID 查詢開發進度或自動推進閉環到下一步。

**Architecture:** 單一 `skills/dev-cycle/SKILL.md`，不建 workflow 對應檔（orchestration skill 特例）。Skill 讀取 filesystem 與 git 狀態判斷當前階段，透過模式偵測區分查詢與推進兩種行為。同步更新 `CLAUDE.md` 補充特例原則、`README.md` 補充 dev-cycle 說明。

**Tech Stack:** 純 Markdown，無 build/test。驗證方式為人工閱讀 Markdown 確認內容正確。

---

### Task 1：建立 `skills/dev-cycle/SKILL.md`

**Files:**
- Create: `skills/dev-cycle/SKILL.md`

- [ ] **Step 1：建立目錄與檔案**

建立 `skills/dev-cycle/SKILL.md`，內容如下：

```markdown
---
description: 以 issue 為中心追蹤並推進開發閉環，支援查詢進度或自動執行下一步
---

你是開發閉環的協調者，負責追蹤 issue 從需求分析到 PR 合併的完整生命週期。

## Input

使用者提供 issue ID，可以是自然語言或指令語氣：
- 查詢：「issue 3396 到哪了」、「3396 進度」、「3396 狀態如何」
- 推進：`/dev-cycle 3396`、「繼續 3396」

若未提供 issue ID，詢問使用者後再開始。

## 模式偵測

輸入含疑問詞或狀態關鍵字（到哪了、狀態、進度、如何、了嗎）→ **查詢模式**
否則 → **推進模式**

## 狀態偵測

依序檢查下列條件，第一個不符合的條件即為當前階段：

| 偵測條件 | 下一步 |
|---|---|
| `docs/issues/issue-{ID}/README.md` 不存在 | `new-issue` |
| `docs/issues/issue-{ID}/` 內無含「Decomposition」標題的 `.md` 檔 | `decompose` |
| 無含 issue ID 的 branch 或 commit（搜尋 branch 名稱與 commit message） | `execute-task` |
| 無 open PR | `create-pr` |
| PR 有 request changes | `execute-task`（修正） |
| PR open，無 review | `review` |
| PR merged | 完成 |

## 查詢模式

偵測當前階段後輸出，然後結束，不推進任何步驟：

> Issue {ID}：{標題（從 README.md 第一個 h1 取得，若無則標記「未知」）}
> 目前階段：{階段名稱}
> 狀態：{一句話摘要}

## 推進模式

1. 偵測當前階段
2. 告知「目前在 [階段]，準備執行 [下一步]」
3. 依階段執行：

   | 階段 | 動作 |
   |---|---|
   | new-issue | 呼叫 `new-issue` |
   | decompose | 呼叫 `decompose` |
   | execute-task | 詢問要執行哪個 Phase / Task 後，呼叫 `execute-task` |
   | create-pr | 確認所有 Task 已完成並已 commit 後，呼叫 `create-pr` |
   | review | 呼叫 `review` |
   | execute-task（修正） | 說明「PR 有 request changes，需修正後重新 commit」，呼叫 `execute-task` |
   | 完成 | 恭喜並提示可以 merge PR，結束 |

4. 子步驟完成後回到步驟 1 繼續偵測
5. 循環直到 PR merged 或使用者中斷
```

- [ ] **Step 2：確認檔案內容正確**

閱讀 `skills/dev-cycle/SKILL.md`，確認：
- frontmatter 只有 `description`，無 `name` 欄位
- 狀態偵測表有 7 列
- 查詢模式與推進模式皆有明確結束條件

- [ ] **Step 3：Commit**

```bash
git add skills/dev-cycle/SKILL.md
git commit -m "feat: 新增 dev-cycle orchestration skill"
```

---

### Task 2：更新 `CLAUDE.md` 補充 Orchestration Skill 原則

**Files:**
- Modify: `CLAUDE.md`（在「Skill ↔ Workflow 配對」段落末尾新增子段落）

- [ ] **Step 1：在 CLAUDE.md 的配對段落末尾新增以下內容**

在這一行之後：

```
只有 frontmatter 與少數平台無關語意差異是允許的，其餘內容應一致。新增 skill 時，務必同時建立兩份。
```

緊接著新增：

```markdown

#### Orchestration Skill 例外

部分 skill 的核心能力是「理解意圖 + 讀取環境狀態 + 呼叫子 skill」，這類 **orchestration skill** 在任何載入其 SKILL.md 的 AI 平台上都能正常運作，不需要 `workflows/shared/` 對應檔。

新增 orchestration skill 時，只建 `skills/<name>/SKILL.md`，**不**建 `workflows/shared/<name>.md`。

目前的 orchestration skill 清單：`dev-cycle`
```

- [ ] **Step 2：確認修改範圍正確**

閱讀修改後的 `CLAUDE.md`，確認：
- 新段落在「Skill ↔ Workflow 配對」章節內、`Frontmatter 格式` 之前
- 原有配對清單與同步指令未被異動

- [ ] **Step 3：Commit**

```bash
git add CLAUDE.md
git commit -m "docs: 補充 orchestration skill 不建 workflow 對應檔的原則"
```

---

### Task 3：更新 `README.md` 補充 dev-cycle 說明

**Files:**
- Modify: `README.md`（在「開發閉環」章節末尾新增說明段落）

- [ ] **Step 1：在 README.md 開發閉環章節末尾新增說明**

在這一行之後：

```
review 發現需要修正時，回到 execute-task 修正後再走一次 commit → PR → review，循環直到通過。
```

緊接著新增：

```markdown

若想以 issue 為中心自動推進整個閉環，可使用 `dev-cycle` skill：輸入 issue ID，AI 自動偵測目前所在階段並執行下一步，循環直到 PR merged。也支援查詢模式（如「issue 3396 到哪了」），只回報進度不推進。
```

- [ ] **Step 2：確認修改範圍正確**

閱讀修改後的 `README.md`，確認：
- 新段落在閉環圖示之後、「使用方式」章節之前
- 閉環圖示與原有說明未被異動

- [ ] **Step 3：Commit**

```bash
git add README.md
git commit -m "docs: 在開發閉環章節補充 dev-cycle skill 說明"
```
